{
  "name": "smart-bill-manager",
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix frontend && cd backend && python -m venv venv && .\\venv\\Scripts\\pip install -r requirements.txt",
    "frontend": "npm --prefix frontend run dev",
    "backend": "cd backend && .\\venv\\Scripts\\python main.py",
    "dev": "echo \"Run 'npm run backend' and 'npm run frontend' in separate terminals.\""
  }
}
